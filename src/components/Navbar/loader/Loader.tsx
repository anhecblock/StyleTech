import './loaderStyled.css';

const Loader = () => {
    return (
        <div className="loader">
            <span className="loader__spinner"> </span>
        </div>
    );
};

export default Loader;
