import './loaderStyled.css';

const Loader = () => {
    return (
        <div className="loader">
            <span className="loader__spinner" data-testid="spinner">
                {' '}
            </span>
        </div>
    );
};

export default Loader;
